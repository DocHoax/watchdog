apiVersion: v1
kind: Service
metadata:
  name: watchdog
  namespace: watchdog-system
  labels:
    app.kubernetes.io/name: watchdog
    app.kubernetes.io/part-of: watchdog
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: watchdog
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
      protocol: TCP
